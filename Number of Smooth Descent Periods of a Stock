class Solution:
    def getDescentPeriods(self, prices):
        total = 1
        current = 1

        for i in range(1, len(prices)):
            if prices[i - 1] - prices[i] == 1:
                current += 1
            else:
                current = 1
            total += current

        return total

        
