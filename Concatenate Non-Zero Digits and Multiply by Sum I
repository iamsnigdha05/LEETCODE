class Solution:
    def sumAndMultiply(self, n):
        digits = []
        digit_sum = 0

        for ch in str(n):
            if ch != '0':
                digits.append(ch)
                digit_sum += int(ch)

        if not digits:
            return 0

        x = int("".join(digits))
        return x * digit_sum
